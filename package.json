{
  "name": "react-cli-repo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepublish": "lerna run prepublish",
    "publish": "lerna run publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.5.1"
  }
}
